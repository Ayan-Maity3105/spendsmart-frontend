# SpendSmart Frontend 💰

A Flutter Web app for expense tracking.

## Tech Stack
- Flutter Web
- HTTP package
- SharedPreferences for JWT storage
- Provider for state management

## Setup
1. Run `flutter pub get`
2. Update baseUrl in services to your backend URL
3. Run `flutter run -d chrome`